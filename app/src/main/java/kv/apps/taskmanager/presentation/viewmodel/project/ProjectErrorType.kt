package kv.apps.taskmanager.presentation.viewmodel.project

enum class ProjectErrorType {
    LOAD_ERROR,
    CREATE_ERROR,
    UPDATE_ERROR,
    DELETE_ERROR,
    FETCH_ERROR,
    MEMBERS_ERROR,
    INVITATION_ERROR,
    NOTIFICATION_ERROR,
    OWNER_ERROR
}