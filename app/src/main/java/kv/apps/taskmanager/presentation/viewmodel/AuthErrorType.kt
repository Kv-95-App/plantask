package kv.apps.taskmanager.presentation.viewmodel

enum class AuthErrorType {
    LOGIN_ERROR,
    REGISTER_ERROR,
    RESET_PASSWORD_ERROR,
    FETCH_USER_ERROR,
    SESSION_ERROR,
    LOGOUT_ERROR
}