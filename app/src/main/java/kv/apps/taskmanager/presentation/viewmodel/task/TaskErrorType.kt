package kv.apps.taskmanager.presentation.viewmodel.task

enum class TaskErrorType {
    LOAD_ERROR,
    ADD_ERROR,
    UPDATE_ERROR,
    DELETE_ERROR,
    FETCH_ERROR,
    FILTER_ERROR,
    TEAM_MEMBERS_ERROR
}